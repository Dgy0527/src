数据包
```
POST /client/login/captcha-sms-web HTTP/2
Host: server.ai.izdax.cn
Content-Length: 96
Sec-Ch-Ua-Platform: "Windows"
Accept-Language: zh-CN,zh;q=0.9
Accept: application/json, text/plain, */*
Sec-Ch-Ua: "Not-A.Brand";v="24", "Chromium";v="146"
Content-Type: application/json
Sec-Ch-Ua-Mobile: ?0
User-Agent: Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/146.0.0.0 Safari/537.36
Origin: https://izdax.cn
Sec-Fetch-Site: same-site
Sec-Fetch-Mode: cors
Sec-Fetch-Dest: empty
Referer: https://izdax.cn/
Accept-Encoding: gzip, deflate, br
Priority: u=1, i

{"phone":"13555566321","captcha_key":"a5c6208f26264dcf9b7cda7baf89b022","captcha_answer":"6DAZ"}
```

# 数据包分析

## 接口:POST /client/login/captcha-sms-web

### 功能:**短信验证码登录接口**

##  请求体:
```
{"phone":"13555566321","captcha_key":"a5c6208f26264dcf9b7cda7baf89b022","captcha_answer":"6DAZ"}
```

### 参数说明

#### phone:手机号
#### captcha_key:图形验证码会话ID(每次获取图形验证码生成)
#### captcha_answer:图形验证码答案(注意：这个包是**图形验证码校验**，不是短信验证码本身；校验通过后才会下发短信登录验证码)

## 可能存在的漏洞点

### 1.图形验证码可复用(高危)
#### 现象:同一个 captcha_key + captcha_answer 多次提交依然校验成功
#### 危害:图形验证码失效,攻击者可以直接跳过图形限制,无限给任意手机号刷短信验证码（短信轰炸）
#### Burp 测试方法:把这个请求多次重放,如果每次返回成功,代表存在漏洞。

### 2`captcha_key`可预测(中危)
#### key如果是时间戳、简单自增数字,可以批量生成key,绕过图形验证码。

### 3.没有手机号频率限制（短信轰炸，高危）
#### 图形验证码校验成功之后,没有对同一个手机号/IP做请求频率限制,短时间大量下发短信。

### 4.图形验证码可以暴力破解（低‑中危）
#### 验证码长度4位,字符集有限,如果无次数限制,可以爆破`captcha_answer`

## 哪种情况不算漏洞

### 1、同一个`captcha_key`使用一次之后立即失效,重放返回验证码错误 → 正常安全设计。

### 2、IP/手机号做了限流,一分钟最多 1 次短信 → 无漏洞

## 现在要做的验证步骤

### 1、将该请求在Burp Intruder/Repeater重放2‑3次

#### 如果第二次依然返回成功:图形验证码可复用,有效漏洞。

#### 如果返回验证码错误:验证码一次性消耗,这里不存在漏洞。

### 2、换不同手机号,看是否可以无限触发短信

## 还可以进行

### 1、看获取图形验证码接口:`captcha_key`是否可预测？观察key是否时间戳、简单递增。

### 2、看真正下发短信的接口:图形校验通过之后,对手机号、IP有没有做频率限制(短时间能不能反复获取短信)。

### 3、浏览网站其他页面:找robots.txt、备份文件、参数反射测试XSS、越权类


# 短信轰炸(无手机号频率限制) 

## 正常安全逻辑:同一个手机号,1 分钟最多1条,1 小时最多3‑5条,一天有上限。超过直接返回 “发送过于频繁，请稍后再试”。

## 漏洞情况:做完图形验证码校验之后,同一个手机号可以源源不断收到短信验证码,没有次数拦截 → 短信轰炸（高危漏洞）

## 测试:不要重放旧包
#### 关键点:`captcha_key`只能用一次,所以每一次发短信,都必须重新获取全新图形验证码+全新`captcha_key`,不能复用旧 key

## Burp操作流程

### 1、第一次:获取图形验证码→拿到新captcha_key、图形答案→提交,短信下发到你手机。

### 2、重新刷新页面,再次拉取图形验证码,拿到一套全新:captcha_key+captcha_answer

### 3、使用新参数再次POST提交短信接口

### 4、观察结果：

#### 漏洞存在:又成功下发短信到你的手机,后端没有返回 “请求过于频繁”。重复3‑4次还能收到短信→存在短信轰炸。

#### 安全:返回类似发送过于频繁,请稍后再试、操作受限→有频率限制,无漏洞。

### 5、不能Repeater直接重复旧数据包,旧 key 已经作废,只会返回 “图形验证码已过期”,这个现象不能用来判断短信频率限制,很多新手在这里踩坑误判